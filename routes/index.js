const { home } = require('./home');
const { users } = require('./users');
const { error } = require('./error');
const { login } = require('./login');


module.exports = {
    home,
    users,
    error,
    login
};